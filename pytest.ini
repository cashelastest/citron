[pytest]
testpaths = tests
asyncio_mode = auto
# The engine and its connection pool are session-scoped, so tests have to share
# the loop that created them; otherwise asyncpg futures end up on another loop.
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
filterwarnings =
    ignore::DeprecationWarning
